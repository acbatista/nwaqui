= simple_form_for @region, remote: true do |f|
  .row
    .col-md-12
      = f.input :name, placeholder: "Insira o nome"
  .row
    .col-md-12
      = f.input :domain, placeholder: "Insira o domínio"
  %hr/
  .row
    .col-md-12
      = f.submit "Salvar imagem", class: 'btn btn-success', data: { disable_with: t(:disable_with)}